import "server-only";

/**
 * Background Steam sync disabled — profile data updates on Steam OAuth login/link only.
 */
export function syncStaleSteamProfilesBackground(): void {
  // no-op
}

export function startSteamProfileBackgroundSync(): void {
  // no-op
}
